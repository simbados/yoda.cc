[tool.poetry]
name = "myapp"
version = "1.0.0"
description = "A sample app"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.28.0"
click = ">=8.0.0,<9.0.0"

[tool.poetry.dev-dependencies]
pytest = "^7.0"
