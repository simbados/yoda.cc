[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "myapp"
version = "1.0.0"
dependencies = [
    "requests>=2.28.0,<3.0.0",
    "click==8.1.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
